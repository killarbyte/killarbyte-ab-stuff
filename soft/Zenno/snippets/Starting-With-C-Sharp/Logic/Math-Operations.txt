//математические операции

decimal tmp = Decimal.Parse(project.Variables["tmp"].Value.Replace(".", ","), System.Globalization.NumberStyles.Any); //Замена запятой на точку для математических операций в C#
tmp = tmp/3; //пример любой математической операции, например деление
return tmp; //возврат значения (при этом внизу кубика должна быть установлена переменная проекта). Либо можно воспользоваться предыдущим кубиком
